// Client-side Firebase (use in React components)
export { auth, db } from "./client"

// Note: Admin SDK should only be imported in server-side code (API routes, Server Components)
// import { adminAuth, adminDb } from "@/lib/firebase/admin"
